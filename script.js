window.onload = function() {

    const fadeInElement = document.querySelector('.img');
  
    fadeInElement.classList.add('visible'); // Trigger the fade-in transition

    // const UpElement = document.querySelector('#Imglg');
    // UpElement.classList.add('vsl'); // Trigger the fade-in transition
  
  };